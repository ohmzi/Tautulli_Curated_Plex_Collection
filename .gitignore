# Local-only config (contains real API keys/tokens/paths) - do NOT commit
config/config.local.yaml
config/config.local.yml

# IDE/editor
.idea/

# Python bytecode/cache
__pycache__/
*.py[cod]

# Runtime outputs (can contain sensitive info and are machine-specific)
data/*
!data/.gitkeep
.idea/

# App runtime outputs (generated DB/data)
apps/data/

# Node / web build artifacts
node_modules/
**/dist/
**/.vite/
coverage/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (keep examples committed)
.env
.env.*
!.env.example

# OS
.DS_Store

# Agent and design documentation (local-only, not for GitHub)
agent.md
design_system.md
UI_COMPONENTS.md
